git tutorial

git add <file_name>
git add .
git add *.md

git commit -m "comment"

git push <to upload to github>
git pull <to download to local>

git clone <website>

git log
q <to quit log mode>
git log --oneline
git diff <logID> -- <file_name>
git checkout <logID> -- <file_name> -> commit

.gitignore
里面写: *.png ie 所有png结尾的文件都ignore